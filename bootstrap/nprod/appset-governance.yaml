apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: governance-nprod
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: 'https://github.com/rdgoarruda/gitops-global.git'
        revision: main
        directories:
          - path: governance/*/overlays/nprod
  template:
    metadata:
      name: 'ocm-policy-{{path.basename}}-nprod'
      annotations:
        argocd.argoproj.io/sync-wave: "0"     # executa após ocm-config-nprod (wave -1)
      labels:
        category: '{{path.basename}}'
        environment: nprod
    spec:
      project: default
      source:
        repoURL: 'https://github.com/rdgoarruda/gitops-global.git'
        targetRevision: main
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: ocm-policies-nprod
      syncPolicy:
        automated: {prune: true, selfHeal: true}
        syncOptions: [CreateNamespace=true, ServerSideApply=true]
