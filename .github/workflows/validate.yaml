name: Validate Kustomize Builds

on:
  pull_request:
    branches: [main]
    paths:
      - 'bootstrap/**'
      - 'config/**'
      - 'governance/**'

jobs:
  kustomize-build:
    name: kustomize build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install kubectl (com kustomize embutido)
        uses: azure/setup-kubectl@v4
        with:
          version: 'v1.32.0'

      - name: Build — bootstrap/ho
        run: kubectl kustomize bootstrap/ho > /dev/null

      - name: Build — bootstrap/pr
        run: kubectl kustomize bootstrap/pr > /dev/null

      - name: Build — config/ho
        run: kubectl kustomize config/ho > /dev/null

      - name: Build — config/pr
        run: kubectl kustomize config/pr > /dev/null

      - name: Build — governance (todas as categorias)
        run: |
          for dir in governance/*/; do
            echo "→ kustomize build $dir"
            kubectl kustomize "$dir" > /dev/null
          done
