apiVersion: cluster.open-cluster-management.io/v1beta2
kind: Placement
metadata:
  name: placement-nprod
  namespace: open-cluster-management
spec:
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchLabels:
            env: nprod
