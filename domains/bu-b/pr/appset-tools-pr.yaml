apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: bu-tools-pr-b
  namespace: argocd
spec:
  generators:
    - matrix:
        generators:
          - git:
              repoURL: 'https://github.com/rdgoarruda/gitops-bu-b.git'
              revision: main
              directories:
                - path: pr/tools/*
          - clusterDecisionResource:
              configMapRef: acm-placement
              labelSelector:
                matchLabels:
                  cluster.open-cluster-management.io/placement: bu-b-placement-pr
              requeueAfterSeconds: 180
  template:
    metadata:
      name: 'bu-b-tool-{{path.basename}}-pr'
      labels:
        domain: bu-b
        environment: pr
    spec:
      project: default
      source:
        repoURL: 'https://github.com/rdgoarruda/gitops-bu-b.git'
        targetRevision: main
        path: '{{path}}'
      destination:
        name: '{{clusterName}}'
        namespace: default
      syncPolicy:
        automated: {prune: true, selfHeal: true}
        syncOptions: [CreateNamespace=true, ServerSideApply=true]
